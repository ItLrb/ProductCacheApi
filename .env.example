# Copy this file to `.env` and adjust the values. `.env` is git-ignored.
# These variables are consumed by docker-compose (compose.yaml).

# MySQL
MYSQL_ROOT_PASSWORD=change-me
MYSQL_DATABASE=productcachedb

# ASP.NET Core environment (Development enables Swagger)
ASPNETCORE_ENVIRONMENT=Development
